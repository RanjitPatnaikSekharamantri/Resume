# AGENTS.md

## Cursor Cloud specific instructions

This repository ("Resume") is a freshly initialized project with only a `README.md` placeholder. There is currently:

- No application code, framework, or dependencies.
- No `package.json`, `requirements.txt`, `Makefile`, or other dependency manifest.
- No Docker, devcontainer, or CI/CD configuration.
- No lint, test, or build scripts.

When code is added, update this section with relevant startup/run caveats, service descriptions, and non-obvious development notes.
